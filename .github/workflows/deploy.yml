name: Publish Python 🐍 Distribution 📦 to PyPI and TestPyPI

on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev

permissions:
  contents: write
  id-token: write

env:
  PYPI_TOKEN: ${{ secrets.PYPI_PASSWORD }}

jobs:
  # SNAPSHOT JOB: 用于 PR 和 dev 分支，生成预发布快照上传 TestPyPI
  snapshot:
    if: github.event_name == 'push' && github.ref == 'refs/heads/dev' || github.event_name == 'pull_request'
    name: Snapshot Release (TestPyPI)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository (Snapshot)
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
          fetch-depth: 0

      - name: Set Up Python (Snapshot)
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install Dependencies (Snapshot)
        run: pip install --upgrade python-semantic-release build

      # 为了在 dev/PR 上生成预发布版本，
      # 我们利用环境变量覆盖配置中 branch 设置为 "dev"
      - name: Run Semantic Release (Snapshot)
        id: snapshot
        env:
          PYPI_TOKEN: ${{ secrets.PYPI_PASSWORD }}
          GITHUB_TOKEN: ${{ github.token }}
          SEMANTIC_RELEASE_BRANCH: "dev"   # 覆盖配置，生成预发布版
        uses: python-semantic-release/python-semantic-release@v9.21.0
        with:
          github_token: ${{ github.token }}
          git_committer_name: "github-actions"
          git_committer_email: "actions@users.noreply.github.com"
          prerelease: "true"

      - name: List build artifacts (Snapshot)
        run: ls -l dist/
      
      - name: Publish Snapshot to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
          skip-existing: true

  # RELEASE JOB: 用于 main 分支，生成正式发布版本上传 PyPI 与 GitHub Release
  release:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    name: Official Release
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-release-${{ github.ref }}
      cancel-in-progress: false
    steps:
      - name: Checkout Repository (Release)
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
          fetch-depth: 0

      - name: Reset Branch to Workflow SHA (Release)
        run: git reset --hard ${{ github.sha }}

      - name: Ensure HEAD is on branch (Release)
        run: |
          git checkout ${{ github.ref }}
          git status

      - name: Set Up Python (Release)
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install Dependencies (Release)
        run: pip install --upgrade python-semantic-release build

      # 正式发布时使用默认配置（branch = "main"）且不传 prerelease参数
      - name: Run Semantic Release (Official)
        id: release
        env:
          PYPI_TOKEN: ${{ secrets.PYPI_PASSWORD }}
          GITHUB_TOKEN: ${{ github.token }}
        uses: python-semantic-release/python-semantic-release@v9.21.0
        with:
          github_token: ${{ github.token }}
          git_committer_name: "github-actions"
          git_committer_email: "actions@users.noreply.github.com"

      - name: Publish GitHub Release Assets (Official)
        if: steps.release.outputs.released == 'true'
        uses: python-semantic-release/publish-action@v9.21.0
        with:
          github_token: ${{ github.token }}
          tag: ${{ steps.release.outputs.tag }}
